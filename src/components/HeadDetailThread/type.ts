export interface ThreadData {
  threadId: number;
  threadTitle: string;
  categoryId: number;
  userId: number;
  threadDate: string; // ISO string for date
  threadDesc: string;
  userName: string;
  categoryName: string;
}
